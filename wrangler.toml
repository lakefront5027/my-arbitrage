name = "lof-arb-radar"
main = "worker-full.js"
compatibility_date = "2024-11-01"

# KV 已完全移除（2026-03 去 KV 化重构）
# 报警状态改用模块级内存变量 _alertState，无需持久化绑定

[triggers]
crons = ["* * * * *", "15 1 * * 1-5"]

[observability]
enabled = true
